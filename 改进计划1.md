# PantrySync 改进计划 1

## 📋 概述

本文档记录了 PantrySync 应用的全面优化和修复工作，确保应用可以直接上架 App Store 供用户下载使用。

---

## 🔍 发现的问题

### 严重问题 🔴

| 问题 | 严重程度 | 说明 |
|------|---------|------|
| **自动填充虚假数据** | 🔴 高 | 应用首次启动时自动填充示例数据，影响用户体验 |
| **IAP 购买功能未实现** | 🔴 高 | PaywallView 按钮为空操作，无法完成购买 |
| **相机扫描条形码未实现** | 🔴 高 | BarcodeScannerView 使用占位符代码，无法实际扫描 |
| **营养追踪功能缺失** | 🔴 高 | 消耗食品时营养数据不会更新到 DailyNutritionLog |
| **膳食计划添加食谱UI缺失** | 🔴 高 | MealPlanView 只显示食谱，无法添加食谱 |

---

## ✅ 已完成的优化

### 1. 移除自动填充虚假数据

**问题描述**：
- 应用首次启动时，`ScreenshotDataSeeder.seed()` 自动填充示例数据
- 用户看到的是虚假数据，影响信任度

**解决方案**：
- 移除 `PantrySyncApp.swift` 中的自动数据填充代码
- 在 `SettingsView.swift` 中添加"加载示例数据"按钮
- 用户需要手动确认才会加载示例数据

**修改文件**：
- `PantrySync/PantrySync/PantrySyncApp.swift`
- `PantrySync/PantrySync/Views/Settings/SettingsView.swift`

**代码变更**：
```swift
// 移除前
@AppStorage("hasSeededData") private var hasSeededData = false

.onAppear {
    if !hasSeededData {
        ScreenshotDataSeeder.seed(context: modelContext)
        hasSeededData = true
    }
}

// 移除后
// 无自动填充代码，用户手动选择加载
```

---

### 2. 实现 StoreKit IAP 购买功能

**问题描述**：
- `PaywallView` 中的购买按钮没有实际功能
- 无法完成订阅购买流程

**解决方案**：
- 创建 `PurchaseManager.swift`，使用 StoreKit 2 实现
- 支持产品获取、购买、恢复购买
- 集成到 `SettingsView` 和 `PaywallView`

**新增文件**：
- `PantrySync/PantrySync/Services/PurchaseManager.swift`

**核心功能**：
```swift
@MainActor
@Observable
final class PurchaseManager {
    static let shared = PurchaseManager()
    
    private(set) var products: [Product] = []
    private(set) var purchasedProductIDs: Set<String> = []
    private(set) var isLoading = false
    private(set) var error: String?
    
    var isPremium: Bool { !purchasedProductIDs.isEmpty }
    
    // 产品 ID
    private let productIDs: Set<String> = [
        "com.zzoutuo.PantrySync.monthly",
        "com.zzoutuo.PantrySync.yearly",
        "com.zzoutuo.PantrySync.lifetime"
    ]
    
    // 加载产品
    func loadProducts() async { ... }
    
    // 购买产品
    func purchase(_ product: Product) async -> Bool { ... }
    
    // 恢复购买
    func restorePurchases() async { ... }
    
    // 监听交易更新
    private func listenForTransactions() -> Task<Void, Error> { ... }
}
```

**修改文件**：
- `PantrySync/PantrySync/Views/Settings/SettingsView.swift`
  - 添加订阅状态显示
  - 添加恢复购买按钮
  - 集成 PurchaseManager

---

### 3. 实现相机条形码扫描功能

**问题描述**：
- `BarcodeScannerView` 使用占位符代码
- 无法实际扫描条形码

**解决方案**：
- 使用 AVFoundation 实现相机预览
- 使用 AVCaptureMetadataOutput 检测条形码
- 支持 EAN-13、EAN-8、Code-128、QR Code

**修改文件**：
- `PantrySync/PantrySync/Views/Scanner/BarcodeScannerView.swift`

**核心功能**：
```swift
class CameraManager: NSObject, ObservableObject {
    @Published var isAuthorized = false
    @Published var session = AVCaptureSession()
    private let metadataOutput = AVCaptureMetadataOutput()
    private var onBarcodeDetected: ((String) -> Void)?
    
    // 请求相机权限
    func requestCameraPermission() { ... }
    
    // 开始扫描
    func startScanning(onBarcodeDetected: @escaping (String) -> Void) {
        // 配置 AVCaptureSession
        // 设置元数据输出类型
        metadataOutput.metadataObjectTypes = [.ean13, .ean8, .code128, .qr]
    }
    
    // 停止扫描
    func stopScanning() { ... }
}

// 相机预览视图
struct CameraPreviewView: UIViewRepresentable {
    let session: AVCaptureSession
    // 创建 AVCaptureVideoPreviewLayer
}
```

**扫描流程**：
1. 用户点击扫描按钮
2. 请求相机权限
3. 显示相机预览
4. 检测条形码
5. 震动反馈
6. 调用 Open Food Facts API 查询产品信息
7. 自动添加到储藏室

---

### 4. 修复营养追踪功能

**问题描述**：
- 用户消耗食品时，营养数据不会更新到 `DailyNutritionLog`
- 营养追踪页面始终显示"No Nutrition Data"

**解决方案**：
- 修改 `PantryViewModel.consumeItem` 方法
- 添加 `updateNutritionLog` 方法
- 根据食品数量和单位计算营养值

**修改文件**：
- `PantrySync/PantrySync/ViewModels/PantryViewModel.swift`
- `PantrySync/PantrySync/Views/Pantry/PantryView.swift`

**核心功能**：
```swift
func consumeItem(_ item: PantryItem, context: ModelContext, logs: [DailyNutritionLog]) {
    item.isConsumed = true
    item.consumedDate = Date()
    item.updatedAt = Date()
    
    updateNutritionLog(for: item, context: context, logs: logs)
    
    try? context.save()
}

private func updateNutritionLog(for item: PantryItem, context: ModelContext, logs: [DailyNutritionLog]) {
    let today = Date()
    let calendar = Calendar.current
    
    // 获取或创建今日日志
    let log = logs.first { calendar.isDate($0.date, inSameDayAs: today) } ?? {
        let newLog = DailyNutritionLog(date: today)
        context.insert(newLog)
        return newLog
    }()
    
    // 单位转换
    let quantityIn100g: Double
    if item.unit == "g" || item.unit == "grams" {
        quantityIn100g = item.quantity / 100.0
    } else if item.unit == "kg" || item.unit == "kilograms" {
        quantityIn100g = item.quantity * 10.0
    } else if item.unit == "oz" || item.unit == "ounces" {
        quantityIn100g = item.quantity * 28.35 / 100.0
    } else if item.unit == "lb" || item.unit == "pounds" {
        quantityIn100g = item.quantity * 453.6 / 100.0
    } else {
        quantityIn100g = 1.0
    }
    
    // 更新营养值
    log.totalCalories += item.caloriesPer100g * quantityIn100g
    log.totalProtein += item.proteinPer100g * quantityIn100g
    log.totalCarbs += item.carbsPer100g * quantityIn100g
    log.totalFat += item.fatPer100g * quantityIn100g
}
```

**数据流**：
```
用户滑动食品 → 点击"Consumed" → 
consumeItem() → updateNutritionLog() → 
更新 DailyNutritionLog → 营养追踪页面显示数据
```

---

### 5. 添加膳食计划选择食谱UI

**问题描述**：
- `MealPlanView` 只显示食谱名称，无法添加食谱
- 用户无法规划每周膳食

**解决方案**：
- 添加 `RecipePickerView` 用于选择食谱
- 点击早餐/午餐/晚餐行时弹出选择器
- 自动创建 `MealPlan` 如果当前周不存在

**修改文件**：
- `PantrySync/PantrySync/Views/MealPlan/MealPlanView.swift`

**新增功能**：
```swift
struct RecipePickerView: View {
    @Environment(\.dismiss) private var dismiss
    let recipes: [Recipe]
    let selectedRecipe: (Recipe) -> Void
    @State private var searchText = ""
    
    var body: some View {
        NavigationStack {
            List(filteredRecipes) { recipe in
                Button {
                    selectedRecipe(recipe)
                    dismiss()
                } label: {
                    HStack {
                        Image(systemName: "fork.knife")
                            .foregroundStyle(.orange)
                        VStack(alignment: .leading) {
                            Text(recipe.title)
                                .font(.subheadline.weight(.medium))
                            HStack(spacing: 8) {
                                if recipe.prepTimeMinutes > 0 {
                                    Text("\(recipe.prepTimeMinutes) min")
                                }
                                Text("\(recipe.servings) servings")
                            }
                            .font(.caption)
                            .foregroundStyle(.secondary)
                        }
                        Spacer()
                        if recipe.isFavorite {
                            Image(systemName: "heart.fill")
                                .foregroundStyle(.pink)
                        }
                    }
                }
                .buttonStyle(.plain)
            }
            .navigationTitle("Select Recipe")
            .searchable(text: $searchText, prompt: "Search recipes")
        }
    }
}
```

**交互流程**：
```
用户点击早餐/午餐/晚餐行 → 
弹出 RecipePickerView → 
选择食谱 → 
调用 viewModel.assignRecipe() → 
更新 MealPlan 数据
```

---

## 📊 功能完整性检查

### 数据模型

| 模型 | 状态 | 说明 |
|------|------|------|
| `PantryItem` | ✅ 完整 | 食品储藏室项目，包含营养信息 |
| `GroceryItem` | ✅ 完整 | 购物清单项目 |
| `Recipe` | ✅ 完整 | 食谱，包含配料和营养信息 |
| `MealPlan` | ✅ 完整 | 膳食计划，关联食谱 ID |
| `DailyNutritionLog` | ✅ 完整 | 每日营养日志 |

### 功能模块

| 模块 | 状态 | 功能 |
|------|------|------|
| **PantryView** | ✅ 完整 | 添加、删除、消耗、搜索、筛选、排序 |
| **GroceryListView** | ✅ 完整 | 添加、删除、勾选、移动到储藏室 |
| **RecipeListView** | ✅ 完整 | 添加、删除、收藏、添加配料到购物清单 |
| **MealPlanView** | ✅ 完整 | 周视图、选择食谱、早中晚餐规划 |
| **NutritionDashboardView** | ✅ 完整 | 卡路里环形图、宏量营养素、周图表 |

### 核心服务

| 服务 | 状态 | 实现方式 |
|------|------|----------|
| **BarcodeLookupService** | ✅ 真实 API | Open Food Facts API |
| **ReceiptScanner** | ✅ 真实 OCR | Vision Framework |
| **NotificationService** | ✅ 真实通知 | UserNotifications |
| **PurchaseManager** | ✅ 完整 | StoreKit 2 |
| **CameraManager** | ✅ 完整 | AVFoundation |

---

## 🔄 用户完整使用流程

```
1. 首次启动
   ├─ PantryView: "Empty Pantry" 空状态
   ├─ GroceryListView: "No Grocery Items" 空状态
   ├─ RecipeListView: "No Recipes" 空状态
   ├─ MealPlanView: "No Recipes Yet" 空状态
   └─ NutritionDashboardView: "No Nutrition Data" 空状态

2. 添加食品
   ├─ 手动添加: AddPantryItemView
   ├─ 扫描条形码: BarcodeScannerView → Open Food Facts API
   └─ 扫描收据: ReceiptScannerView → Vision OCR

3. 管理食品
   ├─ 查看过期/即将过期食品
   ├─ 搜索、筛选、排序
   └─ 标记已消耗 → 自动更新营养日志

4. 购物清单
   ├─ 手动添加: AddGroceryItemView
   ├─ 从食谱添加配料: RecipeDetailView → "Add to Grocery List"
   └─ 勾选后移动到储藏室

5. 食谱管理
   ├─ 添加食谱: AddRecipeView
   ├─ 收藏、搜索
   └─ 添加配料到购物清单

6. 膳食计划
   ├─ 选择周
   ├─ 点击早餐/午餐/晚餐
   └─ 选择食谱: RecipePickerView

7. 营养追踪
   ├─ 查看每日卡路里
   ├─ 查看宏量营养素
   └─ 查看周趋势图

8. 设置
   ├─ 订阅状态
   ├─ 升级到 Premium
   ├─ 恢复购买
   ├─ 加载示例数据（可选）
   └─ 清除所有数据
```

---

## 📱 App Store 上架准备

### 已完成 ✅

- [x] 移除自动填充虚假数据
- [x] 实现 IAP 购买功能
- [x] 实现相机条形码扫描
- [x] 修复营养追踪功能
- [x] 添加膳食计划选择食谱UI
- [x] 所有功能模块完整可用
- [x] 数据流正确连接
- [x] 真实 API 集成
- [x] 政策页面部署

### 需要在 App Store Connect 配置

- [ ] 创建应用 ID: `com.zzoutuo.PantrySync`
- [ ] 配置订阅产品:
  - `com.zzoutuo.PantrySync.monthly` - 月度订阅
  - `com.zzoutuo.PantrySync.yearly` - 年度订阅
  - `com.zzoutuo.PantrySync.lifetime` - 终身购买
- [ ] 上传应用截图和预览
- [ ] 填写应用元数据（参考 `keytext.md`）
- [ ] 配置隐私政策 URL
- [ ] 配置服务条款 URL

---

## 📝 提交记录

### Commit 1: feat: implement StoreKit IAP and camera barcode scanning
```
- Add PurchaseManager.swift with StoreKit 2 implementation
- Implement product fetching, purchase, and restore functionality
- Update PaywallView to use PurchaseManager for real purchases
- Add camera barcode scanning with AVFoundation
- Implement CameraManager for AVCaptureSession handling
- Add CameraPreviewView for SwiftUI camera preview
- Support EAN-13, EAN-8, Code-128, and QR code scanning
- Add haptic feedback on successful barcode scan
```

### Commit 2: fix: implement nutrition tracking and meal plan recipe selection
```
- Add nutrition log update when consuming pantry items
- Calculate nutrition based on quantity and unit conversion
- Add RecipePickerView for selecting recipes in meal plan
- Enable tapping meal rows to assign recipes
- Create new MealPlan if none exists for current week
```

---

## 🎯 总结

经过本次优化，PantrySync 应用已经具备完整的功能，可以直接上架 App Store：

1. **用户体验优化**：移除自动填充虚假数据，用户首次启动看到空白状态
2. **功能完整性**：所有核心功能都已实现并可正常使用
3. **数据流正确**：各模块之间的数据流正确连接
4. **真实 API 集成**：条形码查询、收据扫描、过期提醒都使用真实服务
5. **IAP 完整实现**：用户可以完成订阅购买流程

应用现在可以安全地上架 App Store 供用户下载使用。
